pytest>=8.0.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
# jq is not a pure-Python wheel on many systems — see notes below.
# If you need python bindings, consider `pyjq` or install system jq (apt / apk).
# jq>=1.6.0
